PSC:
	BEEP
		Makes a noise.
	CLEARSCREEN
		Clears the console.
	COMMENT <comment>
		Comments a line.
	CONCAT <var1> <var2> <var3>
		Assigns var1 + var2 to var3.
	DIR <folder>
		Changes the working directory.
	EXIT <exitcode>
		Closes this instance.
	LOG <message>
		Outputs a message to a log file.
	LOGVAR <variable>
		Outputs a variable to a log file.
	MAKEFOLDER <folder>
		Creates a folder.
	OUTPUT <message>
		Outputs a message to the console.
	OUTPUTVAR <variable>
		Outuputs a variable to the console.
	READ <variable>
		Reads from the console into a variable.
	SETVAR <variable> <value>
		Sets a variable.
	STARTPROCESS <process>
		Starts a process.
PSMC:
	SOUND
		Makes a noise.
	CLEAR
		Clears the console.
	CONCATENATE
	<var1>
	<var2>
	<var3>
		Assigns var1 + var2 to var3.
	EXIT
	<exitcode>
		Closes this instance.
	LOG
	<variable>
		Outputs a variable to a log file.
	CREATEFOLDER
	<folder>
		Creates a folder.
	CONSOLE
	<variable>
		Outputs a variable to the console.
	USERVARIABLE
	<variable>
		Reads from the console into a variable.
	VARIABLE
	<variable>
	<value>
		Sets a variable.
	START
	<process>
		Starts a process.
